<% if signed_in? %>
    <% if current_user.admin? %>

<div class="admin">
  <ul class="nav nav-pills">
    <li><%= link_to t('.new_post'), new_post_path %></li>
    <li> <%= link_to t('.users'), users_path %></li>
    <li> <%= link_to t('.my_questions'), questions_path %></li>
    <li>  <%= link_to t('.my_letters'), letters_path %></li>
  </ul>
</div>
  <br/>

<% else %>
        <div class="alert alert-error">
          <h4 class="alert alert-error">Внимание!</h4>
          У вас нет прав войти в административную область
        </div>
<% end %>
<% end %>